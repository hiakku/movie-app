import { createTheme } from "@mui/material/styles";

let theme = createTheme({
  palette: {
    primary: {
      main: "#273244",
      normal: "#D4D7DD",
    },
    secondary: {
      main: "#1F2A3C",
    },
  },
});

export default theme;
